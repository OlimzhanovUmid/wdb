## 1. Gate + toolchain bump (prereq for CHR)

- [x] 1.1 Launch the bundled JBR 21.0.11 with `-XX:+AllowEnhancedClassRedefinition -version` on a wall machine and verify it starts without rejecting the flag; if rejected, swap in a full JetBrains Runtime with DCEVM before proceeding. **DONE** — verified DCEVM present: `AllowEnhancedClassRedefinition` appears as a `{product}` flag in `-XX:+PrintFlagsFinal` on both the source JBR 21.0.11 (`jbr_jcef-...-b1163.116`) and the runtime bundled in the built agent app-image (`-nomod`, ships to wall); flag accepted with exit 0. Stock OpenJDK lacks this flag, so its presence confirms a full JBR with enhanced redefinition.
- [x] 1.2 Bump the repo-wide Kotlin version (catalog `kotlin`) to the CHR-required ≥2.1.20 and add the Compose Multiplatform + Compose-compiler + CHR Gradle plugins to the catalog; verify the whole build still compiles (`./gradlew compileKotlin compileTestKotlin`) after the bump. **DONE** — Kotlin 2.0.21→2.1.21; added compose 1.10.0, composeHotReload 1.2.0, compose-compiler plugins; settings got pluginManagement + google() repo; whole build compiles.
- [x] 1.3 Verify `org.jetbrains.compose.hot-reload:hot-reload-orchestration` and `:hot-reload-agent` resolve from Maven Central at the pinned version and can be consumed from a plain Kotlin/JVM module; verify a trivial import compiles in wdb-agent. **DONE** — orchestration + core (its public API leaks core types) added to wdb-agent; ChrHotReloadCoordinator compiles against the real 1.2.0 API (startOrchestrationServer / port.getBlocking / sendBlocking / ReloadClassesRequest/Result). agent jar resolved into an off-classpath `hotReloadAgentJar` config for bundling.

## 2. Protocol: RELOAD stream

- [x] 2.1 Add `StreamKind.RELOAD` to `wdb-protocol` Wire.kt and verify the existing protocol round-trip test still passes with the new kind.
- [x] 2.2 Add reload messages to Messages.kt: a `ReloadBatch` (list of `{ relPath, changeType: ADDED|MODIFIED|REMOVED, bytes? }` + batch sha256) and a `ReloadResult` (outcome `APPLIED|FAILED`, reason?); verify serialization round-trips in a unit test including a REMOVED entry with no bytes. Bytes travel as raw blob frames (PUSH/AGENT_UPDATE pattern), not inline JSON; added `ErrorCode.HOT_RELOAD_REQUIRED`.

## 3. Client: reload op + class diff

- [x] 3.1 Add `sendReload(addr, batch)` to `wdb-client` ClientOps.kt and `WdbClient.reload(...)`, mirroring `sendAgentUpdate`; verify a unit test drives it against a stub agent server and gets a `ReloadResult`. `ReloadPayload` = batch manifest + bytesByPath; FakeAgent got a RELOAD branch; tests cover APPLIED + FAILED.
- [x] 3.2 Add a class-snapshot/diff helper (hash each `.class` under a dir, diff vs a previous snapshot → added/modified/removed); verify unit tests cover add, modify, delete, and no-change cases. `ClassDiff` (snapshot/diff/buildPayload/batchSha256) in Reload.kt; forward-slash relpaths; buildPayload returns new baseline snapshot for the watch loop.

## 4. Agent: hot launch mode

- [x] 4.1 Bundle `hot-reload-agent`, `hot-reload-runtime-jvm`, `hot-reload-orchestration` (`org.jetbrains.compose.hot-reload` 1.2.0) into the agent package and verify the jars land in the agent app-image. **DONE** — orchestration + core land in installDist/lib via runtimeClasspath (verified); the `-javaagent` jar is resolved into an off-classpath config and copied into the image by `packageAgent`; `detectHotReloadAgentJar()` finds it beside the agent jars and `Main` sets `config.hotReloadAgentJar`. (runtime-jvm is the app's dep via the CHR plugin, not the agent's.) jpackage bundling itself is exercised on the wall (task 8).
- [x] 4.2 Extend `Supervisor` with a hot-run launch path: allocate a loopback orchestration port + a per-run hot-classpath dir, add the `-javaagent`, `-XX:+AllowEnhancedClassRedefinition`, and `compose.reload.*` system properties, prepend the hot dir to the classpath; verify a unit test asserts the built command contains the hot flags only when hot mode is requested and the normal command is byte-for-byte unchanged otherwise. **DONE** — `buildLaunchCommand` extracted + `HotLaunch` threaded through launch/restart/auto-restart; `HotLaunchCommandTest` covers hot flags + byte-for-byte normal command.
- [x] 4.3 Track hot-vs-normal state for the running app and expose it in status; verify status reports hot mode after a hot launch and not after a normal launch. **DONE** — `RunningApp.hot` + `MachineStatus.hotMode` + `isHotRunning()`/`currentHotLaunch()`; the post-launch status flag is exercised live in task 8 (needs a real process).

## 5. Agent: orchestration host + reload apply

- [x] 5.1 Host a CHR `OrchestrationServer` on the allocated loopback port for the lifetime of a hot run; verify (unit/integration) the app's hot-reload-agent can connect to it locally. **DONE (compile-verified)** — `ChrHotReloadCoordinator.beginHotRun()` starts the server (`startOrchestrationServer`), reads the loopback port (`port.getBlocking().leftOrNull()`), returns the `HotLaunch`. Real app-connect is a live check (task 8) — no local CHR app to connect in a unit test.
- [x] 5.2 Add a `RELOAD` handler in `AgentServer`: verify batch integrity (sha256), reject if the current run is not hot (error) or integrity fails (FAILED, app untouched), else write added/modified bytes into the hot dir at their paths, delete removed, emit a `ReloadClassesRequest(paths)`, and reply with `ReloadResult`. Serialize applies under a per-app lock. Verify unit tests for: non-hot rejection, corrupt-batch rejection leaving the app untouched, and a successful apply writing the expected files + emitting the request. **DONE** — logic extracted to injectable `ReloadApplier` (REJECTED for not-hot / integrity / zip-slip; APPLIED via coordinator); `ReloadApplierTest` covers all four cases. Handler drains blob frames, shares `mutationLock`.

## 6. CLI: `wdb reload`

- [x] 6.1 Add a `reload` Clikt command (`reload <classesDir> [machine] --all --host --watch`): one-shot pushes the current diff; `--watch` watches the dir and pushes incremental deltas per target; verify a unit/integration test does a one-shot reload against a stub agent and asserts only changed classes are sent. **DONE** — `Reload` command with per-target snapshot baselines + 500ms watch loop; only-changed-classes assertion covered by `ClientOps`/`ClassDiff` tests (buildPayload delta + sendReload batch).
- [x] 6.2 Implement fan-out with per-machine results (reuse the `push --all` pattern) and the redeploy fallback: on a `FAILED` ReloadResult for a machine, trigger that machine's full deploy+restart; verify a test where one target returns FAILED falls back to redeploy for that target only and others still report applied. **DONE** — `reloadOrRedeploy` (client) returns Applied/Rejected/Redeployed/Failed; REJECTED does NOT redeploy, FAILED does; `ReloadTest` verifies fallback fires only on FAILED. CLI fans out over targets independently with `--fallback-jar`.

## 7. Test fixture: CHR in wdb-dummy-app

- [x] 7.1 Convert `wdb-dummy-app` from a plain Kotlin/JVM uber-jar into a minimal Compose Desktop app (add Compose plugin + a `@Composable` window showing a visibly editable label), then add the CHR Gradle plugin + `hot-reload-runtime-jvm`; verify it builds and runs normally (non-hot) unchanged. (Added scope: dummy-app had no Compose UI before.) **DONE** — added Compose Desktop + compose-compiler + CHR plugins; new `dev.wdb.dummy.hot.HotAppKt` Compose window (single editable label); the headless heartbeat `MainKt` stays the jar's Main-Class so agent tests are unchanged (verified green with the 30MB Compose-fat jar). NOTE: `DevelopmentEntryPoint{}` (CHR's reloadable-scope wrapper) is a dev-runtime-variant symbol only wired by the plugin's `hotRun` path; since wdb launches the app itself, whether an explicit wrapper is required is confirmed during live bring-up (task 8).

## 8. Live verification on the wall

- [x] 8.1 Deploy the CHR-enabled dummy-app to a wall machine, start it in hot mode, edit a visible composable on the dev machine, run `wdb reload --watch`, and verify the change appears on the physical display without the window closing or the app restarting. **DONE — live on wall-02.** Shipped CHR agent 0.2.1 via self-update; `push --main-class dev.wdb.dummy.hot.HotAppKt` + `run --hot` → hot:true; edited label v1→v2→v3, `wdb reload` each time → `reloaded 4 class(es)` (CHR ReloadClassesResult isSuccess), text changed live on the physical display, restarts:0, window never closed. Required fix: bundle the SHADOWED (`-standalone`) hot-reload-agent jar — the thin one's premain died with NoClassDefFoundError on its own deps.
- [x] 8.2 Force an unsupported change (e.g. alter a class supertype) and verify the agent reports FAILED and the CLI falls back to a full redeploy+restart that brings the app back with the new code. **DONE — live on wall-02.** A class-supertype swap turned out to be hot-applied fine by DCEVM 1.2 (more capable than assumed); the reliable trigger is a throwing top-level `val` (CHR re-runs static init on reload → FAILS). Result: `reload failed -> redeployed (ok=true, restarted=true)`, app RUNNING, restarts:0, no error in logs. **Found + fixed a real bug the wall exposed:** a FAILED reload left its poison classes in the hot dir, and since hot launch puts `-cp "<hotDir>;<jar>"` the stale class shadowed the redeployed jar → crash loop. Fix: a new deployment clears the hot dir (its deltas belong to the old jar). Verified recovery is clean after the fix.
- [x] 8.3 Run `wdb reload --all` against two wall machines and verify both update independently with per-machine results, including one where the app is not in hot mode being reported as rejected without affecting the other. **DONE — live.** `reload --all` over wall-02 (hot) + wall-03 (not hot): wall-02 → `reloaded 4 class(es)` (label updated live), wall-03 → `rejected (app is not running in hot-reload mode)`, app untouched; independent, neither aborted the other.
